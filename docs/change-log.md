# 修改记录

## 记录协议

格式：
- 修复：`序号.修复【范围】：存在 xxx 问题，修复后 xxx。`
- 其他：`序号.类型【范围】：原行为/新增内容 xxx，调整/新增后 xxx。`

类型：修复 / 新增 / 调整 / 删除 / 文档 / 测试

规则：
- 本文件仅记录正式代码的变更；方案文档、原型、设计稿等非正式开发产物及其调整不记录
- 每条只记录一个变化
- 修复类必须描述“原问题 + 修复后结果”
- 其他类型描述“原行为/新增内容 + 最终效果”
- 不记录排查过程
- 不写大段实现细节
- 序号持续递增，不重新编号

## 记录

1.修复【默认模型】：存在 Issue2PR 错误回退到 `deepseek-v4-pro` 的问题，修复后统一使用 DSH 当前 Default 模型。
2.修复【P10 降级】：存在 LLM/API 失败被误判为 JSON 解析错误并重试的问题，修复后保留基础失败报告并降级返回。
3.修复【超时取消】：存在 LLM/DSH agent 超时后仍可能持续等待的问题，修复后 LLM 会中断底层 stream，DSH agent 不再被 `whenIdle()` 阻塞。
4.修复【P6 builtin】：存在 Coder 并发无上限且无效 diff 可能进入后续流程的问题，修复后最大并发限制为 2，并在写入前校验 unified diff。
5.修复【P6 外部执行】：存在无代码变更任务被强制生成空 patch 并导致整阶段失败的问题，修复后按 `patched` / `no_change` / `failed` 记录任务结果，仅 `patched` 生成并校验真实 diff。
6.调整【UI 改造】：原界面以项目、运行及配置面板组织，调整后按 v9 统一明暗主题与电脑端自适应布局，提供项目、任务、全局设置及阶段工作区；接入任务配置快照，完善复核、执行观测、文件证据、交付、助手及异常恢复，并清理过期界面代码。
7.修复【任务视图】：存在查看某节点时整体验证、交付页签与阶段侧栏混入其他节点内容，未触及阶段渲染零占位区块，上一轮补丁混入本轮验证的问题，修复后按节点留空（仅一条等待空态）、任务级页签按进度门禁、补丁只列本轮有效文件并标注上一轮残留、复核历史移入任务详情、文件页隐藏流程图，并增加查看其他阶段的提示条。
8.修复【工作台入口】：存在宿主 conversation 锚点缺失时点击 Issue2PR 入口静默渲染空白且不恢复的问题，修复后按轮询回退视口居中布局并在页面顶部显示提示条，锚点恢复后自动重新贴合。
9.调整【任务视图】：原"整体验证/交付"任务级页签与节点视图并列、任意节点下展示跨阶段内容，调整后节点产物完全隔离（默认跟随当前阶段、完成后默认最后节点、手动选择即暂停跟随），交付与全流程验证汇总并入 P11 节点并明确标注跨阶段归属，补丁 Diff 并入 P6 节点，"文件"页签更名"全部产物"。
10.调整【节点内容】：原执行日志、阶段产物以折叠块藏在节点视图底部、不滚动难以发现，调整后各节点内容盘点置顶为带计数的节点内子页签（如 P11"交付｜全流程汇总"、P6"执行实例｜变更文件 N"），无内容的区块不出页签，删除全部底部折叠块。
11.调整【阶段返回入口】：原"返回当前阶段"按钮在拓扑摘要、流程底部、节点空态、内容横条四处重复出现，调整后删除提示横条与全部重复按钮，仅在展开流程底部保留原型同款纯文字两态——跟随中显示"跟随当前阶段 ✓"，偏离时显示"正在查看 X · 返回当前 Y →"（点击返回并恢复跟随），等待复核时仍由复核条承担。
12.新增【进度可视化】：对照 v9 原型补齐缺失的进度信息，新增阶段耗时（流程节点悬停与标题旁）、重跑"待重验"节点标记、节点标题轮次（第 N 轮）、P8 测试输出子页签、变更文件 +/− 统计、P10 历史记录三态、复核页脚关注点与下一步提示。
13.调整【重复元素收敛】：原阶段文件列表（侧栏与子页签）、P11 结论（门禁网格与汇总卡）、PR 说明入口、阶段状态话术、产物命名多处重复或不一致，调整后删除"阶段证据"侧栏（职责契约并入任务详情）、汇总移除交付评测卡、交付移除查看文件按钮、检查卡状态话术单一源，实例内产物更名"本实例产物"。
14.调整【任务详情排版】：原任务头部三行按钮分散、节点视图含静态描述行且标题与子页签占两行，调整后"任务详情"并入更多操作菜单、删除静态描述行（阶段错误以红色提示条按需显示）、子页签并入标题行居右、任务编号悬停显示复制提示。
15.优化【渲染展示】：原 Diff 视图无行号、日志终端整段纯文本无计数与着色、结构化报告深层嵌套直接渲染裸 JSON，调整后 Diff 按旧/新文件行号双列显示，日志终端按行渲染并显示"共 N 行/匹配 M"计数且错误行标红，深层嵌套改为可折叠"展开 N 项"节点。
16.新增【阶段模型与执行器展示】：原节点视图与流程图不显示各阶段使用的模型与执行器，调整后运行信息显示在任务页签行"执行过程"之后并随节点切换——按三分支语义显示（外部 Agent 执行显示委托方式不显示模型；内置执行显示模型，阶段覆盖优先标注并回落启动快照；P7 补丁管线无模型调用、P8 显示测试执行命令），执行器文案与新建任务弹窗统一单一源。
17.调整【任务头部】：原任务标题下的状态说明行与状态徽章、复核条、失败分析条等信息重复，调整后删除该行（各状态信息由对应载体承载），任务头部由三行减为两行。
18.优化【委外日志展示】：原 P6 外部 agent 的执行输出为原始 stream-json 单行帧难以阅读，调整后逐帧 JSON 美化（缩进展开）展示，非 JSON 行原样保留，纯遥测帧（thinking_tokens 计数心跳）滤除不显示，不做语义映射与统计；原始日志经"查看源文件"不变。
19.修复【委外日志超限】：原真实长任务的 external-exec.log 超过 200KB 读取上限后界面完全看不到执行过程（退回阶段事件兜底），调整后 artifact 接口支持 tail=1 尾部读取（返回末尾片段并标记 truncated），委外日志始终尾部读取并在界面标注仅显示尾部。
20.新增【日志全量搜索】：原搜索只能作用于已读取的尾部内容，调整后搜索框输入时自动以 full=1 加载完整文件（硬顶 5MB）在全量中过滤，界面标注"· 全量"并提示加载状态。
21.新增【委外时间线】：原委外执行仅有原始 stream-json 帧日志（external-exec.log）单行难读，新增写入时同步格式化"时间|kind|内容"行落到 external-exec.timeline.log（原始日志保真不动，镜像写失败静默不阻断执行），P6 执行过程默认展示人读时间线，原始帧日志经"查看源文件"仍保真可见。
22.新增【全局设置存储】：原复核方式、执行器等执行配置存于各项目 project.json，新增全局 settings.json 持久化统一承载（复核方式、执行器、测试命令、打回次数上限、阶段提示词覆盖，revision 随保存递增），多页面并发保存按 revision 检出冲突并提示重新载入，项目内旧配置仅作无快照历史任务兼容读取。
23.修复【门禁与状态叙事】：存在完成任务 P11 六项验收门禁"通过/未通过"渲染完全相同、卡片三层成功信号与未通过事实自相矛盾、折叠进度条失败段冒充进行中的问题，修复后门禁值按结果分色加图标（绿✓通过/红✕未通过）、存在未通过时标题并列"N 项门禁未通过"警示章且尾部改汇总式"6 项门禁：X 通过 / Y 未通过"（全通过任务仍纯成功态）、折叠进度条失败阶段改红色段。
24.修复【工作台交互与信息密度】：存在"•••"菜单展开后无 Esc/点击外部关闭、重开工作台焦点停在关闭按钮、失败页两种进度叠加、运行元信息混入页签行且页签位置随页签切换跳变、两条失败提示重复同一句、打回次数生效值不可见、助手输入区窄占位截断禁用无解释、面包屑显示全量任务编号、产物预览与分页无行号、提示词编辑器无计数与覆盖标识、超 200KB 产物打不开的问题，修复后菜单受控开关（Esc/外点关闭、焦点回菜单锚点）、面板根承接初始焦点、展开轨道时隐藏折叠进度条且元信息降为页签下独立行、页签位置恒定、失败条只留结论与动作、打回次数显式回显生效值（默认 3）、助手面板加宽且占位缩短禁用态有解释、任务编号超宽省略悬停见全量、预览与分页注入不可选中行号、编辑器常显字符计数与"已覆盖默认值"标识、超限产物自动尾部读取并标注"尾部 N 行"。
