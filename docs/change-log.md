# 修改记录

## 记录协议

格式：
- 修复：`序号.修复【范围】：存在 xxx 问题，修复后 xxx。`
- 其他：`序号.类型【范围】：原行为/新增内容 xxx，调整/新增后 xxx。`

类型：修复 / 新增 / 调整 / 删除 / 文档 / 测试

规则：
- 本文件仅记录正式代码的变更；方案文档、原型、设计稿等非正式开发产物及其调整不记录
- 每条只记录一个变化
- 修复类必须描述“原问题 + 修复后结果”
- 其他类型描述“原行为/新增内容 + 最终效果”
- 不记录排查过程
- 不写大段实现细节
- 序号持续递增，不重新编号

## 记录

1.修复【默认模型】：存在 Issue2PR 错误回退到 `deepseek-v4-pro` 的问题，修复后统一使用 DSH 当前 Default 模型。
2.修复【P10 降级】：存在 LLM/API 失败被误判为 JSON 解析错误并重试的问题，修复后保留基础失败报告并降级返回。
3.修复【超时取消】：存在 LLM/DSH agent 超时后仍可能持续等待的问题，修复后 LLM 会中断底层 stream，DSH agent 不再被 `whenIdle()` 阻塞。
4.修复【P6 builtin】：存在 Coder 并发无上限且无效 diff 可能进入后续流程的问题，修复后最大并发限制为 2，并在写入前校验 unified diff。
5.修复【P6 外部执行】：存在无代码变更任务被强制生成空 patch 并导致整阶段失败的问题，修复后按 `patched` / `no_change` / `failed` 记录任务结果，仅 `patched` 生成并校验真实 diff。
