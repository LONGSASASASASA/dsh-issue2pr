# 修改记录

## 记录协议

格式：
- 修复：`序号.修复【范围】：存在 xxx 问题，修复后 xxx。`
- 其他：`序号.类型【范围】：原行为/新增内容 xxx，调整/新增后 xxx。`

类型：修复 / 新增 / 调整 / 删除 / 文档 / 测试

规则：
- 本文件仅记录正式代码的变更；方案文档、原型、设计稿等非正式开发产物及其调整不记录
- 同一次修改及其配套测试合并为一条记录，独立修改分别记录
- 修复类必须描述“原问题 + 修复后结果”
- 其他类型描述“原行为/新增内容 + 最终效果”
- 不记录排查过程
- 不写大段实现细节
- 序号持续递增，不重新编号

## 记录

1.修复【默认模型】：存在 Issue2PR 错误回退到 `deepseek-v4-pro` 的问题，修复后统一使用 DSH 当前 Default 模型。
2.修复【P10 降级】：存在 LLM/API 失败被误判为 JSON 解析错误并重试的问题，修复后保留基础失败报告并降级返回。
3.修复【超时取消】：存在 LLM/DSH agent 超时后仍可能持续等待的问题，修复后 LLM 会中断底层 stream，DSH agent 不再被 `whenIdle()` 阻塞。
4.修复【P6 builtin】：存在 Coder 并发无上限且无效 diff 可能进入后续流程的问题，修复后最大并发限制为 2，并在写入前校验 unified diff。
5.修复【P6 外部执行】：存在无代码变更任务被强制生成空 patch 并导致整阶段失败的问题，修复后按 `patched` / `no_change` / `failed` 记录任务结果，仅 `patched` 生成并校验真实 diff。
6.调整【UI 改造】：原界面以项目、运行及配置面板组织，调整后按 v9 统一明暗主题与电脑端自适应布局，提供项目、任务、全局设置及阶段工作区；接入任务配置快照，完善复核、执行观测、文件证据、交付、助手及异常恢复，并清理过期界面代码。
7.修复【任务视图】：存在查看某节点时整体验证、交付页签与阶段侧栏混入其他节点内容，未触及阶段渲染零占位区块，上一轮补丁混入本轮验证的问题，修复后按节点留空（仅一条等待空态）、任务级页签按进度门禁、补丁只列本轮有效文件并标注上一轮残留、复核历史移入任务详情、文件页隐藏流程图，并增加查看其他阶段的提示条。
8.修复【工作台入口】：存在宿主 conversation 锚点缺失时点击 Issue2PR 入口静默渲染空白且不恢复的问题，修复后按轮询回退视口居中布局并在页面顶部显示提示条，锚点恢复后自动重新贴合。
9.调整【任务视图】：原"整体验证/交付"任务级页签与节点视图并列、任意节点下展示跨阶段内容，调整后节点产物完全隔离（默认跟随当前阶段、完成后默认最后节点、手动选择即暂停跟随），交付与全流程验证汇总并入 P11 节点并明确标注跨阶段归属，补丁 Diff 并入 P6 节点，"文件"页签更名"全部产物"。
10.调整【节点内容】：原执行日志、阶段产物以折叠块藏在节点视图底部、不滚动难以发现，调整后各节点内容盘点置顶为带计数的节点内子页签（如 P11"交付｜全流程汇总"、P6"执行实例｜变更文件 N"），无内容的区块不出页签，删除全部底部折叠块。
11.调整【阶段返回入口】：原"返回当前阶段"按钮在拓扑摘要、流程底部、节点空态、内容横条四处重复出现，调整后删除提示横条与全部重复按钮，仅在展开流程底部保留原型同款纯文字两态——跟随中显示"跟随当前阶段 ✓"，偏离时显示"正在查看 X · 返回当前 Y →"（点击返回并恢复跟随），等待复核时仍由复核条承担。
12.新增【进度可视化】：对照 v9 原型补齐缺失的进度信息，新增阶段耗时（流程节点悬停与标题旁）、重跑"待重验"节点标记、节点标题轮次（第 N 轮）、P8 测试输出子页签、变更文件 +/− 统计、P10 历史记录三态、复核页脚关注点与下一步提示。
13.调整【重复元素收敛】：原阶段文件列表（侧栏与子页签）、P11 结论（门禁网格与汇总卡）、PR 说明入口、阶段状态话术、产物命名多处重复或不一致，调整后删除"阶段证据"侧栏（职责契约并入任务详情）、汇总移除交付评测卡、交付移除查看文件按钮、检查卡状态话术单一源，实例内产物更名"本实例产物"。
14.调整【任务详情排版】：原任务头部三行按钮分散、节点视图含静态描述行且标题与子页签占两行，调整后"任务详情"并入更多操作菜单、删除静态描述行（阶段错误以红色提示条按需显示）、子页签并入标题行居右、任务编号悬停显示复制提示。
15.优化【渲染展示】：原 Diff 视图无行号、日志终端整段纯文本无计数与着色、结构化报告深层嵌套直接渲染裸 JSON，调整后 Diff 按旧/新文件行号双列显示，日志终端按行渲染并显示"共 N 行/匹配 M"计数且错误行标红，深层嵌套改为可折叠"展开 N 项"节点。
16.新增【阶段模型与执行器展示】：原节点视图与流程图不显示各阶段使用的模型与执行器，调整后运行信息显示在任务页签行"执行过程"之后并随节点切换——按三分支语义显示（外部 Agent 执行显示委托方式不显示模型；内置执行显示模型，阶段覆盖优先标注并回落启动快照；P7 补丁管线无模型调用、P8 显示测试执行命令），执行器文案与新建任务弹窗统一单一源。
17.调整【任务头部】：原任务标题下的状态说明行与状态徽章、复核条、失败分析条等信息重复，调整后删除该行（各状态信息由对应载体承载），任务头部由三行减为两行。
18.优化【委外日志展示】：原 P6 外部 agent 的执行输出为原始 stream-json 单行帧难以阅读，调整后逐帧 JSON 美化（缩进展开）展示，非 JSON 行原样保留，纯遥测帧（thinking_tokens 计数心跳）滤除不显示，不做语义映射与统计；原始日志经"查看源文件"不变。
19.修复【委外日志超限】：原真实长任务的 external-exec.log 超过 200KB 读取上限后界面完全看不到执行过程（退回阶段事件兜底），调整后 artifact 接口支持 tail=1 尾部读取（返回末尾片段并标记 truncated），委外日志始终尾部读取并在界面标注仅显示尾部。
20.新增【日志全量搜索】：原搜索只能作用于已读取的尾部内容，调整后搜索框输入时自动以 full=1 加载完整文件（硬顶 5MB）在全量中过滤，界面标注"· 全量"并提示加载状态。
21.新增【委外时间线】：原委外执行仅有原始 stream-json 帧日志（external-exec.log）单行难读，新增写入时同步格式化"时间|kind|内容"行落到 external-exec.timeline.log（原始日志保真不动，镜像写失败静默不阻断执行），P6 执行过程默认展示人读时间线，原始帧日志经"查看源文件"仍保真可见。
22.新增【全局设置存储】：原复核方式、执行器等执行配置存于各项目 project.json，新增全局 settings.json 持久化统一承载（复核方式、执行器、测试命令、打回次数上限、阶段提示词覆盖，revision 随保存递增），多页面并发保存按 revision 检出冲突并提示重新载入，项目内旧配置仅作无快照历史任务兼容读取。
23.修复【门禁与状态叙事】：存在完成任务 P11 六项验收门禁"通过/未通过"渲染完全相同、卡片三层成功信号与未通过事实自相矛盾、折叠进度条失败段冒充进行中的问题，修复后门禁值按结果分色加图标（绿✓通过/红✕未通过）、存在未通过时标题并列"N 项门禁未通过"警示章且尾部改汇总式"6 项门禁：X 通过 / Y 未通过"（全通过任务仍纯成功态）、折叠进度条失败阶段改红色段。
24.修复【工作台交互与信息密度】：存在"•••"菜单展开后无 Esc/点击外部关闭、重开工作台焦点停在关闭按钮、失败页两种进度叠加、运行元信息混入页签行且页签位置随页签切换跳变、两条失败提示重复同一句、打回次数生效值不可见、助手输入区窄占位截断禁用无解释、面包屑显示全量任务编号、产物预览与分页无行号、提示词编辑器无计数与覆盖标识、超 200KB 产物打不开的问题，修复后菜单受控开关（Esc/外点关闭、焦点回菜单锚点）、面板根承接初始焦点、展开轨道时隐藏折叠进度条且元信息降为页签下独立行、页签位置恒定、失败条只留结论与动作、打回次数显式回显生效值（默认 3）、助手面板加宽且占位缩短禁用态有解释、任务编号超宽省略悬停见全量、预览与分页注入不可选中行号、编辑器常显字符计数与"已覆盖默认值"标识、超限产物自动尾部读取并标注"尾部 N 行"。
25.调整【流程轨道】：原流程节点间距大、连线自节点圆心画至下一圆心占屏过宽，调整后轨道 gap 缩小为 2px、节点内边距压缩、连线两端各内缩 14px 避开圆点边缘，节点带更紧凑。
26.调整【委外时间线】：原时间线写入仅含时分秒、跨天任务无法溯源日期，调整后写入完整 `YYYY-MM-DD HH:MM:SS`，界面展示剥离日期只显时分秒，旧格式（无日期前缀）兼容解析与过滤。
27.新增【P2 检索候选卡片】：原 P2 结果走通用键值表渲染、文件与证据分散难读，新增按候选文件卡片呈现——文件、职责、置信度分档（高/中/低三色 pill）与证据段落相邻，测试候选与待探索项折叠收纳，保留"查看结果文件"入口。
28.新增【P11 交付警示与证据跳转】：原交付页六门禁只有通过/未通过徽章、失败后无从下手，新增存在未通过项时顶部警示条（已结束任务显示"流程已结束，交付验收尚未通过 · N 项未通过"）、未通过门禁行内"查看证据 ↗"入口（根因→P4 假设、补丁→补丁账本、测试/审查/说明→对应报告，验收门禁→全流程汇总），补丁应用未通过直显 patchEvidence 校验失败的具体原因行。
29.新增【P6 外部执行事件表】：原外部执行器输出为纯文本时间线（整段文本、无结构无法筛选），新增四列事件表——时间/类型（kind 原值）/名称（工具名，缺失显示 —）/内容预览（120 码点截断悬停见全文），34px 单行、日志区域内横滚；整行点击就地展开完整内容（镜像 500 字上限触顶明示）；类型下拉按已载入事件动态生成、「异常」开关按内容错误关键字筛选（明示派生方式）、搜索自动升级全量；旧运行无时间线镜像回退原文本输出，内置多智能体实例视图不变。
30.新增【P6 单轮结构与事件详情弹窗】：原外部实例视图为"最终回复块 + 折叠输出"结构、事件只能行内展开不便复制与对照原始记录，调整后外部实例按单轮呈现——输入（任务包快照内嵌，无则不渲染）→ 执行过程（事件表）→ 输出（最终回复 + 关联产物链接）；事件详情改独立弹窗——详情视图按正文分区并支持分区复制/复制全部，原始记录视图显示含日期镜像行（截断上限明示）并可跳完整原始日志，弹窗内搜索行级命中高亮 + 上一个/下一个导航。
31.新增【全部产物弹窗与单层产物打开】：原产物浏览只有整页「全部产物」页签、事件详情中「完整原始日志」点击后跳离当前上下文，新增阶段标题右侧「全部产物 N ↗」单层弹窗（复用文件面板左树右预览，弹窗内切换文件，宽弹窗上限提至 960px），事件详情中打开产物改为当前弹窗内替换为源文件预览（超限自动尾部读取）并提供「← 返回事件」返回，整页页签保留。
32.调整【任务头部】：原任务详情收在更多菜单且无任务说明，调整后常驻任务详情入口，标题下仅展示已有 P1 现象字段；缺失或与标题重复时不占位，其他任务操作保留。
33.调整【紧凑阶段导航】：原收起流程仅显示不可操作的小进度条，调整后常显十个可选阶段，保留失败、待复核、当前与选中状态；展开仍显示完整节点及 P10 入口，两态均可返回当前阶段。
34.调整【阶段信息布局】：原运行元信息、阶段标题和节点页签分散排列，调整后标题、状态、实际执行信息与全部产物入口同排，节点页签移到下一行，整页产物视图保留；阶段头固定在滚动内容区上方，窄屏区域横滚。
35.调整【单轮执行记录】：原输入、过程和输出缺少轮次视觉分组，调整后用圆点、竖线和第 1 轮标题组织本次外部执行，展示实际状态和起止时间，筛选栏紧凑排列；仍不支持多轮补充输入。
36.修复【阶段次数语义】：存在把人工打回次数加一显示为第 N 轮、容易与实际执行次数或对话轮次混淆的问题，修复后仅在有打回记录时显示「已打回 N 次」。
37.优化【单实例观测】：唯一外部 Agent 直接内嵌执行记录，省去实例列表和无意义筛选；不自动写入显式实例选择，保留当前阶段复核入口，多实例与其他上下文的复核隔离不变。
38.优化【执行信息密度】：外部状态集中到轮次头，完整会话编号、起止时间、退出码与复制移入实例详情；输入优先展示目标段落或 TaskGraph 节点标题，完整任务包仍可查看。
39.新增【最新记录跟随】：Claude 执行中自动跟随最新记录，上翻、筛选、展开或查看输出时暂停，新增记录计数可一键返回最新；显示最近记录时间，DSH 明示结束后提供记录。同次执行刷新保留已读日志，重跑重新取数；超 200KB 时间线读取尾部，全量搜索失败保留记录并可重试，历史回填不计新增。
40.优化【日志滚动】：事件表随工作区滚动、表头吸顶，取消默认日志内纵向滚动；新增专注查看，与事件详情单层切换，保留筛选并恢复入口焦点；查看输出可直达本轮结果。
41.调整【观测导航】：区分 Agent 执行记录与流水线日志，取消重复任务视图页签；全部产物统一从阶段标题打开，弹窗支持展开整页，保留文件选择和返回现场。
42.优化【输出产物】：补丁和报告优先展示，任务包、原始日志等辅助证据收进更多文件；任务、阶段及实例状态语义保持独立。新增 10 项 UI 回归，全量 283 项通过，并以真实组件和隔离数据验证桌面、窄屏、深色、连续追加记录及复核返回。
43.修复【P11 评测依据】：原最终评测仅凭 PR 说明重判六门禁，可能把已验证的补丁、测试和审查误判为失败；修复后 PATCH 取真实应用校验、TEST 取测试报告、DIFF 取审查报告与实际补丁校验，模型负责对照真实证据判断 ROOT/DESC/ACCEPT。说明生成与验收均接收需求、根因、账本、补丁、测试、审查及打回意见；报告增加 schemaVersion、评测时间和逐项 reasons，未执行的评测为 null，不连带将六项全部标红。
44.修复【验收放行】：原内置 P11 写入 fail 报告后仍能正常返回并自动完成，人工复核也未检查六门禁；新增统一交付报告校验，内置执行返回、人工批准及最终完成前必须六项全 pass 且 PR 说明非空。委外 P11 复用同一校验，JSON null 等无效报告不得放行；评测中断不再保留上轮全 pass 结果。
45.修复【完成状态误导】：历史 completed 任务的头部、P11 标题和流程节点按真实验收报告显示通过/未通过/待确认，报告缺失、异常、过期及读取中不显示绿色成功；P11 复核按钮同步拦截未通过或未知结果，任务列表中性显示流程已结束。逐项展示失败原因，旧报告无原因明确提示重新评测，不自动改写历史结论。
46.修复【补丁截断误述】：原 P9 将截取片段标为 Diff 全文，可能导致审查与 PR 说明误述原补丁损坏；P9/P11 现在逐文件明确完整或上下文节选及字符数，说明节选不代表原文件截断。验证：全量 315 项通过，真实组件桌面/768px 深色检查失败、通过、未知和未评测状态；未重跑真实任务或改写其历史产物。
47.统一【产物弹窗】：全部产物、阶段文件、补丁、输入包、日志源文件、验收证据和复核历史均使用同一文件弹窗，点击即定位；取消整页分支及旧文件页偏好，原阶段正文持续挂载。详情与文件单层替换，关闭文件先返回来源；文件缺失明确提示，不误开其他文件。
48.统一【日志读取与搜索】：阶段事件、P8 测试输出和 P6 旧日志回退共用读取逻辑，超过 200KB 自动读尾部，搜索按需补读全量；阶段事件全量仍按阶段筛选。读取失败保留同轮内容、入口与重试；切任务、重跑、文件删除/截短清旧快照。显示实际复制下载范围，搜索超过 600 条匹配可翻页；全量仍遵守服务端 5MB 上限。
49.统一【阅读现场】：普通文本日志随工作区滚动，专注模式独立滚动，统一搜索、换行、暂停/跟随最新与新记录提示。修复专注日志打开源文件再返回丢失滚动位置；详情或专注查看时暂停阶段自动切换，流程推进不会卸载正在阅读的现场。
50.修复【执行信息】：非 P6 委托也根据实际 external 标记或启动快照显示“外部会话 · 过程未采集”，避免误标宿主模型，启动快照优先于当前项目配置。阶段耗时优先取可靠起止时间，不从长日志尾部片段推算整个阶段耗时。
51.调整【导航与文案】：流水线日志改为标题右侧“阶段事件”按钮，弹窗查看当前阶段的平台记录，读取失败时保留入口；“变更文件 N”改为“补丁 N”，预览内展示涉及的源码文件数。统一“打开源文件”和“原文/排版”，ACCEPT 跳汇总明确叫“查看汇总”，同步空态指引。
52.验证【全局一致性】：UI 75 项、全量 327 项通过；隔离浏览器 9 组检查通过，覆盖公共弹窗、完整输入、事件与专注阅读返回、阶段文件定位、P8 长日志全量搜索、阶段事件边界、768px 深色及 Esc 焦点恢复、报告筛选与文档链接。根目录报告补充实施结果，宿主验收表保持未检查；未验证真实 DSH 登录态，未运行真实 Agent 或改写历史任务。
53.调整【日志默认五条】：Agent 事件表、P8 输出、各阶段事件弹窗及 P6 旧日志回退，执行中和已结束均默认最后 5 条事件或 5 行文本；新增只更新预览窗口，不滚动整页。展开/详情/文件/搜索时暂停并保留阅读现场，新增单独提示，跟随最新恢复五条窗口。
54.统一【完整记录检索】：专注查看按需补读历史，从末页开始按每页 50 条浏览；搜索、类型与异常筛选覆盖完整记录，匹配从第一页显示，不受五条预览限制。阶段事件弹窗内直接切换最近五行与历史，不叠加弹窗。明确 Agent 搜索时间线镜像、P8 搜索测试输出、阶段事件限定当前阶段，保留 5MB 全量上限及失败重试提示。
55.验证【最近五条】：UI 81 项、全量 333 项通过；7 组隔离浏览器检查通过，覆盖实时追加不滚整页、展开及历史快照冻结、默认五条、早期记录检索、每页 50 条、阶段事件单层弹窗、旧任务文本回退、超长行省略和 768px 深色。未运行真实 Agent，未验证已登录 DSH 宿主。
56.新增【Agent 原始消息】：Claude 与 DSH 执行新增 external-exec.messages.jsonl 留存实际完整 JSON，external-exec.events.jsonl 保存独立摘要及消息位置、长度、校验值和本次执行编号；同帧多个内容块共享原文，重跑不复用事件编号。DSH 在正常、异常及超时回收前保存本轮原生事件，仍在执行结束后提供记录。
57.修复【Claude 流式留档】：原先按 stdout 分片直接解析时间线，跨分片 JSON 和中文字符可能被拆坏；现按 UTF-8 边界和完整行缓冲，结束时处理无换行的完整末帧，保留旧原始日志，观测文件写失败不使任务崩溃。
58.新增【原始消息读取】：增加只读 agent-event 接口，按事件编号验证消息边界、校验值及摘要后返回原始 JSON；工具结果仅按同次执行的真实工具标识关联。只读固定任务来源，单条超过 5MB、校验失败、缺失或关联歧义均明确提示，不截断后宣称完整。
59.修复【事件原始 JSON】：原“原始记录”仅显示时间线摘要，缺少 Agent 返回字段；现详情按原始消息展示完整正文、工具参数及结果，“原始 JSON”支持折叠、搜索全部字段和复制完整消息，关联结果独立切换。保持最近五条预览及源文件单层弹窗，补读时保留正在阅读的消息。
60.调整【历史记录兼容】：旧 Claude 记录仅在原始 stdout 中严格唯一匹配时显示完整 JSON；相同摘要有多个候选、文件缺失、截断或未采集原生数据时说明原因，平台生成事件明确没有 Agent 原始消息，不将摘要包装为 JSON，不改写历史任务。
61.测试【Agent 原始 JSON】：全量 364 项通过，其中 UI 85 项；5 组真实采集器/只读接口/页面组件的隔离浏览器联调通过，另 7 组最近五条策略回归通过，覆盖字段保真、工具关联、搜索复制、单层返回、历史缺失与窄屏深色。未运行真实 Agent，未验证已登录 DSH 宿主。
62.修复【阶段耗时】：存在运行中没有新阶段事件时耗时停在 0 秒的问题，修复后按本轮开始时间每秒更新，结束及停止后固定耗时，人工复核等待单独显示；断线冻结在上次确认时间，重跑不沿用旧轮次时间。
63.新增【Claude 当前活动】：原推理计数被事件表过滤后无法判断是否仍有活动，新增 P6 Claude Code 活动摘要，区分最近信号与最近动作，聚合推理心跳并展示真实进程核对状态；静默 60 秒提示无新活动、180 秒补充判断依据，不因静默自动停止任务。
64.新增【后台任务观测】：原依赖安装等后台任务的启动与完成通知不可见，新增独立于推理状态的后台任务摘要，可展开命令、起止时间、已收到的结果及退出码；提交不等于完成，缺少结束证据时保留未确认状态。
65.调整【活动采集边界】：新增节流活动快照及只读接口附加信息，按执行编号与阶段开始时间隔离重跑；原始消息继续保留，心跳不刷事件表、不反复写任务状态。旧执行仅按日志更新时间降级展示，本轮未扩展 P8 测试输出和 DSH 原生 Agent 的实时采集。
66.修复【活动快照刷新】：存在快照原子替换暂时失败后、没有新消息就不再刷新的问题，修复后活跃期自动退避重试，结束时有限重试保存终态，清理后不继续写入，也不无限等待影响任务结束。
67.测试【活动与耗时观测】：全量 389 项通过，其中 UI 89 项、活动采集 20 项；真实 P6 执行器与只读 HTTP 接口的隔离联调确认执行中可见、任务文件不随心跳改写及重跑隔离，11 组真实页面组件的隔离浏览器检查覆盖计时、后台任务、阅读现场、静默提示及 1440/1024px 明暗主题。原宿主仍加载旧版且 Day.js 任务在执行，本轮未重启宿主、调用真实模型或修改该任务。
68.修复【P8 实时输出】：存在测试结束后才保存输出、执行期间无法查看过程的问题，修复后启动即建立本轮日志，持续保存 stdout 和 stderr，保留中文分片及失败、超时前输出，结束时不重复追加；测试报告继续保留退出码、通过状态和末尾摘要。
69.新增【P8 执行观测】：原 P8 默认只有待生成结果的空态，新增当前命令、最近输出时间、执行状态与结束退出码，默认进入测试输出；没有新输出时按真实进程状态提示，断线明确待确认，报告生成后保留正在阅读的输出，沿用最近五行、暂停跟随、历史搜索和源文件入口。
70.修复【P8 重跑观测】：存在重跑时旧文件被当作本轮输出、旧结果页签在新报告生成后抢回阅读现场的问题，修复后按阶段开始时间和执行编号隔离日志与报告，新轮重置结果页签；输出创建失败不展示旧文件，迟到旧执行不覆盖新产物，记录保存失败明确显示且不宣称测试通过。
71.测试【P8 实时观测】：全量 406 项通过，其中 UI 92 项、P8 实时执行新增 13 项；新增真实子进程与 HTTP 接口联调，11 组正式前后端的隔离浏览器检查覆盖 stdout/stderr、首条输出、静默、断线、失败与超时、重跑、长日志搜索和 1440/1024px 明暗主题。真实 Day.js 任务仅只读分析，本轮未更改其测试脚本、运行配置或模型凭据，未扩展 DSH 原生 Agent 实时采集。
72.修复【Markdown 文件查看】：存在“查看完整输入”等公共文件入口未传宿主 Markdown 必需文案、遇到代码块或脚注导致页面渲染异常的问题，修复后补齐复制、已复制与脚注文案，保留排版、原文切换及完整内容。
73.修复【助手 Markdown 回答】：存在助手回答遗漏同组必需文案、包含代码块或脚注时可能触发页面异常的问题，修复后与文件阅读器共用稳定文案，正常显示流式回答内容。
74.测试【Markdown 渲染兼容性】：全量 408 项通过，其中 UI 94 项；真实宿主 Markdown 组件的隔离浏览器检查确认任务完整输入的打开、原文/排版切换、复制与关闭返回，以及助手代码块和脚注回答均正常，无页面异常；助手响应使用本地模拟，不调用模型。
75.修复【P6 完成门禁】：存在外部执行因限额等错误中断、报告缺失或任务结果不完整时，仅凭已有补丁仍被放行的问题，修复后明确错误或非零退出码不再视为完成，自动推进、人工复核及后台监听统一校验报告完整覆盖任务图、无失败或未完成节点且补丁可用，中断时保留已有补丁与原始错误。全量 413 项测试通过，随后新增监听回归及相关 11 项测试通过；真实 Day.js 任务只读复验确认缺报告时拒绝放行，未重跑原任务。
76.修复【重跑失败提示】：存在重跑后残留上轮失败摘要、运行中仍显示黄色失败条及 P10“已生成”的问题，修复后重跑清除当前失败摘要，页面仅在任务失败时显示失败条，旧报告保留为可查阅的历史记录，并兼容旧任务残留字段。全量 415 项测试通过，覆盖重跑接口、不同任务状态的提示显示、历史入口交互及再次失败显示新分析；未停止或重跑当前真实任务。
77.修复【补丁路径解析】：存在报告中的绝对路径被重复添加目录前缀、导致真实补丁误报不存在的问题，修复后任务目录内绝对路径及两种相对路径统一为任务相对路径，继续拒绝越界及盘符相对路径，原报告不改写。全量 417 项测试通过，覆盖 P6 验证、P7 应用、P11 证据校验与回滚；真实 Day.js 任务只读复验确认 5 份补丁可定位且报告结构验证通过，未重跑原任务。后端修改需由 DSH 加载新版插件并重启生效。
